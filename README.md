# can_test_TX

STM32F767ZI（Nucleo-144系ボード）をベースにした、オムニ/メカナム駆動の走行ロボット制御ファームウェア。プロポ（送信機）からのSBUS入力、CANバス経由の外部コマンド、2基のLidar距離センサーを組み合わせて、走行とローラー機構（吸引/収集用）を制御する。

## 概要

- **駆動方式**: 4輪メカナム駆動（前左/前右/後左/後右）
- **操作入力**: SBUS（RCプロポ、UART5、100000bps / 偶数パリティ / ストップビット2、信号反転）
- **外部連携**: CANバス（CAN1）で外部ノードから8バイトのコマンド/フィードバックを受信
- **センサー**: TF-Luna系Lidar距離センサー×2（UART4・UART7、DMA受信）による壁沿い自動追従
- **アクチュエータ**: 駆動モーター4基（TIM4）＋ローラー/付属モーター4基（TIM1）、電磁弁2系統
- **安全機構**: SBUS/CANの信号断検知による緊急停止、駆動とローラーの同時動作インターロック、ステータスLED

## 走行モード（`Rtuno`スイッチで切替）

| `Rtuno` | モード | 内容 |
|---|---|---|
| `1` | 手動モード | ジョイスティック入力（`m1`〜`m4`）をそのままモーターへ出力 |
| `0` | CAN補正モード | Lidar PIDによる回頭補正（`auto_rx`）とジョイスティックを合成 |
| `-1` | 全自動モード | 2つのLidar距離から前後（`auto_ly`）・回頭（`auto_rx`）を自動算出し、目標距離500mmで壁に対して平行かつ一定距離を保持 |

## ローラー機構（`Lmayu` / `Ltuno`スイッチで切替）

- `Lmayu == 1`：ローラー動作中。`Ltuno`で速度/方向を選択（`1`=通常速度、`-1`=低速「バケツ」モード、`0`=停止）。リミットスイッチ（`stop_flag`）検知後は800ms待ってから停止（惰性動作の猶予）。
- `Lmayu == 0`：待機状態（フラグ類をリセット）。
- `Lmayu == -1`：強制停止。
- リミットスイッチ（`reset_flag`）検知で自動的に排出/リセット動作（スピン逆転）を行い、完了検知（`set_flag`）で停止する自動シーケンスあり。

## グローバル変数

### 入力・スティック系
| 変数 | 説明 |
|---|---|
| `rx, ly, ry, lx` | SBUSチャンネル0〜3から変換したスティック値（±1000、デッドバンド処理済み） |
| `Lmayu, Rmayu` | 左右3段スイッチ（CH4/CH5）：ローラーモード選択／電磁弁選択 |
| `Ltuno, Rtuno` | 左右3段スイッチ（CH6/CH7）：ローラー速度・方向選択／走行モード選択 |

### 駆動系
| 変数 | 説明 |
|---|---|
| `m1〜m4` | ジョイスティックから計算したメカナムミックス値（前左/前右/後左/後右）、最大速度制限のため×0.7 |
| `PV1〜PV6` | CAN受信データ（`use_data`）から変換した目標値（PID的フィードバックのターゲット）。PV1〜4は駆動輪、PV5・PV6はローラーモーター用 |
| `pwm1〜pwm8` | 実際にPWM出力するduty値（TIM4=駆動輪、TIM1=ローラー/付属モーター） |
| `dir1〜dir4` | 駆動モーター4基分の回転方向GPIO状態 |
| `maxpwm` | PWM上限値（700、最大速度を制限） |
| `maxmv` | 1制御周期あたりのPWM変化量上限（150、急激な加減速を抑制） |

### ローラー・機構系
| 変数 | 説明 |
|---|---|
| `stop_flag, reset_flag, set_flag` | リミットスイッチ（`lock6/lock7/lock8`）の状態。ローラーの停止・排出・完了シーケンス制御に使用 |
| `roller_dir` | ローラー回転アクチュエータの回転方向 |
| `timer_flag`, `time3` | リミットスイッチ検知後の800ms待機タイマー制御用 |

### CAN・Lidar系
| 変数 | 説明 |
|---|---|
| `use_data[8]` | CAN受信割り込みで更新される最新の8バイトペイロード |
| `distance4, distance7` | Lidarセンサー1（UART4）・2（UART7）が計測した距離（mm） |
| `auto_ly, auto_rx` | Lidar PID（`auto_mode`）が算出する仮想ジョイスティック出力（前後・回頭） |
| `last_can_rx` | 最後にCANを受信した時刻。CAN断線検知に使用 |
| `SBUS_CH[16]`, `SBUS_Failsafe`, `SBUS_LostFrame` | SBUSデコード結果と信号異常フラグ |

## 主要関数

| 関数 | ファイル | 説明 |
|---|---|---|
| `motor_control(SV, PV, maxMV, down_pwm, max_pwm, *pwmm, *dirr)` | function.c | 汎用モーター制御。目標値と現在値の差からPWMを比例制御し、上限速度・変化率制限、方向反転時の減速シーケンスを行う。全8モーターで共通利用 |
| `roller(void)` | function.c | ローラー機構のステートマシン。`Lmayu`/`Ltuno`とリミットスイッチに応じてローラー・スピン動作を制御 |
| `auto_mode(distance1, distance2, reset_flag, target_dist)` | function.c | 2つのLidar値からPID制御で前後（距離維持）・回頭（平行維持）指令を算出 |
| `safety(void)` | function.c | SBUS/CAN信号断の監視、緊急停止（全PWM0）、駆動とローラーの同時動作禁止インターロック、ステータスLED制御 |
| `CAN_TX(recipient)` | function.c | CAN送信関数（現状は固定ペイロードで、メインループからは未使用。今後の拡張用スタブ） |
| `HAL_CAN_RxFifo0MsgPendingCallback` | function.c | CAN受信割り込み。StdId `0x001`・DLC≥8のフレームを`use_data[]`へ格納 |
| `sbus(void)` | sbus_handler.c | SBUS入力をスイッチ/スティック値に変換し、メカナムミックス`m1〜m4`を計算 |
| `get_switch_state(ch_value)` | sbus_handler.c | SBUSチャンネル値を3段階スイッチ状態（-1/0/1）に変換 |
| `process_stick(ch_value)` | sbus_handler.c | SBUSチャンネル値をスティック値（-1000〜1000）に変換（デッドバンド付き） |
| `lidar(void)` | lidar_sensor.c | DMAリングバッファからLidarの距離データフレーム（ヘッダ`0x5C`）を検出・パース |

## SBUSチャンネル割り当て

| チャンネル | 変数 | 用途 |
|---|---|---|
| CH0 | `rx` | 回頭スティック |
| CH1 | `ly` | 前後スティック |
| CH2 | `ry` | （処理はされるが現状の駆動ミックスには未使用） |
| CH3 | `lx` | 左右（ストレイフ）スティック |
| CH4 | `Lmayu` | ローラーモード選択スイッチ |
| CH5 | `Rmayu` | 電磁弁選択スイッチ |
| CH6 | `Ltuno` | ローラー速度・方向選択スイッチ |
| CH7 | `Rtuno` | 走行モード選択スイッチ |

## 安全機構

- SBUS信号断（`SBUS_CH[0]==0`または`SBUS_LostFrame`）、またはCAN受信断（最終受信から100ms以上経過）を検知すると、全PWM出力を強制的に0にする。
- ローラーモーター（`pwm5`/`pwm6`）が動作中は、駆動輪モーター（`pwm1〜4`）を強制的に0にするインターロックあり。
- ステータスLED3基でSBUS断・CAN断の状態を点滅表示。

## ハードウェア構成（.iocより）

- MCU: STM32F767ZITx（LQFP144）
- CAN1、UART4/UART5/UART7（DMA受信）、USART2/USART3、TIM1・TIM4（各4chPWM）
- USART3は`printf`のデバッグ出力先としてリターゲット
